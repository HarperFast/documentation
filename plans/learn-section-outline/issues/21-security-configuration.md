**Section:** Administration. Configuring Harper's security features (CORS, SSL) and how settings affect runtime behavior.

## Goals / Scope
- Walk through CORS and SSL config with concrete before/after request examples.
- Use real `curl`/`fetch` examples to show the impact of each setting.

## Reference source material
- `reference/security/configuration.md`, `reference/security/overview.md`, `reference/http/configuration.md`, `reference/http/tls.md`
